function npix = ltdeg2pix( a, myScreenInfo )% Converts visual angle in degrees to number of pixels %% npix = ltdeg2pix( a, myScreenInfo )% where a is a (vector of) visual angles%% replaces ltangle2pix and ltdeg2pix%% 2000-01 TCBF% 2000-04 MC % 2011-02 MC added support for objects -- for backward compatibility% 2011-06 ND updated to make it even more backwards compatibleif ~isobject(myScreenInfo) % old way where myScreenInfo is a structure    if ~isfield(myScreenInfo,'Dist') || ~isfield(myScreenInfo,'PixelSize')        error('Screen structure should have fields Dist and PixelSize');    end    npix = 2 * myScreenInfo.Dist/ myScreenInfo.PixelSize * tan( pi/180 * a/2 );else % it is an object, then call the method associated    npix = myScreenInfo.Deg2Pix(a);end