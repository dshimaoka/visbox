function a = ltpix2deg( npix, myScreenInfo)% Converts pixel coordinates into visual angles in degrees %% a = ltpix2deg( npix, myscreen )% where a is a (vector of) pixel coordinates%% replaces pix2deg and ltpix2deg%% 2001-03 MC % 2011-02 MC made essentially obsolete % 2011-06 ND updated and made backward compatibleif ~isobject(myScreenInfo) % old way where myscreen is a structure    if ~isfield(myScreenInfo,'Dist') || ~isfield(myScreenInfo,'PixelSize')        error('Screen structure should have fields Dist and PixelSize');    end    a = 2 * 180/pi*atan(npix / (2 * myScreenInfo.Dist/ myScreenInfo.PixelSize));else % it is an object, then call the method associated    a = myScreenInfo.Pix2Deg(npix);end