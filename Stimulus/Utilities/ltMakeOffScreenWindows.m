function stim = ltMakeOffScreenWindows(myscreen, stim, keep)
% Used in the past to put images into offscreen memory (now a dummy)
% now it is obsolete (replaced by vsLoadTextures)
% all arguments are ignored -- just passes stim through
%
% part of VisBox

% 1999-12 Felix Huerlimann
% 2004-01 MC added support for DEMO condition
% 2006-11 JBB deactivated function, now only exists for compatibility

return
